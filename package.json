{
  "name": "Search-test",
  "version": "1.0.0",
  "description": "Search-test",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "npm run build"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
