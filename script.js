require('dotenv').config();

const apiKey = process.env.API_KEY;
const clientId = process.env.CLIENT_ID;

console.log("API Key:", apiKey);
console.log("Client ID:", clientId);

// ここで API 呼び出しなどの処理を行います
